import 'package:flutter/material.dart';

class DropdownBtnCovid extends StatelessWidget {
  const DropdownBtnCovid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
